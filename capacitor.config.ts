import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marianabuilds.charito',
  appName: 'Project Charito',
  webDir: 'dist'
};

export default config;
